[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile.railway"

[deploy]
startCommand = "--config proxy_server_config.railway.yaml --port 4000"
healthcheckPath = "/health"
healthcheckTimeout = 300

[[services]]
name = "litellm"

[services.env]
PORT = "4000"
