[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "--port 4000"
healthcheckPath = "/health"
healthcheckTimeout = 300

[[services]]
name = "litellm"

[services.env]
PORT = "4000"
